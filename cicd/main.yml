stages:
    - build
    - validate
    - plan
    - apply

include:
    - local: 'cicd/job_templates.yml'
    - local: 'cicd/dev.yml'
    - local: 'cicd/test.yml'
    - local: 'cicd/PROD.yml'

variables:
    gitlab_env: "$CI_ENVIRONMENT_NAME"
